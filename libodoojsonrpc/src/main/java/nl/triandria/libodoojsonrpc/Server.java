package nl.triandria.libodoojsonrpc;

/**
 * Holds all the information the library needs to know about the server we are connecting to.
 */
class Server {

    // TODO insert the necessary attrs you need (url, db, uname, pwd) find out the version yourself
    Server() {

    }
}
